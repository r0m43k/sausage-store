name: Backend Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      APP_VERSION: '1.0'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Configure Maven toolchains for JDK 17
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/toolchains.xml <<'XML'
          <toolchains xmlns="http://maven.apache.org/TOOLCHAINS/1.1.0"
                      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                      xsi:schemaLocation="http://maven.apache.org/TOOLCHAINS/1.1.0 https://maven.apache.org/xsd/toolchains-1.1.0.xsd">
            <toolchain>
              <type>jdk</type>
              <provides>
                <version>17</version>
                <vendor>temurin</vendor>
              </provides>
              <configuration>
                <jdkHome>${env.JAVA_HOME}</jdkHome>
              </configuration>
            </toolchain>
          </toolchains>
          XML

      - name: Show effective Java for Maven
        run: mvn -B -q -Dorg.slf4j.simpleLogger.log.org.apache.maven.plugins.toolchain.DefaultToolchainManager=debug validate

      - name: Verify Java & Maven
        run: |
          java -version
          mvn -v

      - name: Build with Maven (skip tests)
        working-directory: backend
        run: mvn -B package -DskipTests=true -Dversion.application=${APP_VERSION}.${GITHUB_SHA:0:7}

      - name: Run Integration Tests
        working-directory: backend
        run: mvn -B test

      - name: Run Checkstyle
        working-directory: backend
        run: mvn -B checkstyle:checkstyle

      - name: Run SpotBugs
        working-directory: backend
        run: mvn -B spotbugs:check
        continue-on-error: true

      - name: Upload JAR Artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: backend/target/*.jar
